export default defineNuxtRouteMiddleware(async (to, from) => {
  // const response = await $fetch("/api/auth");
  // if (!useCookie("token")) {
  //   return navigateTo("/login");
  // if (!useCookie("token").value && to.path !== "/login") {
  //   console.log("Move to login p111age");
  //   return navigateTo("/login");
  // }
});
